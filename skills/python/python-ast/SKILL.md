---
name: python-ast
description: "Program with Python's ast module: The `ast` module helps Python applications to process trees of the Python abstract syntax grammar."
version: 1.0.0
tags: [ast, programming, python, stdlib]
---

# Python: `ast`

## Overview

The `ast` module helps Python applications to process trees of the Python
abstract syntax grammar.  The abstract syntax itself might change with
each Python release; this module helps to find out programmatically what
the current grammar looks like and allows modifications of it.

An abstract syntax tree can be generated by passing `ast.PyCF_ONLY_AST` as
a flag to the `compile()` builtin function or by using the `parse()`
function from this module.  The result will be a tree of objects whose
classes all inherit from `ast.AST`.

A modified abstract syntax tree can be compiled into a Python code object
using the built-in `compile()` function.

Additionally various helper functions are provided that make working with
the trees simpler.  The main intention of the helper functions and this
module in general is to provide an easy to use interface for libraries
that work tightly with the python syntax (template engines for example).

:copyright: Copyright 2008 by Armin Ronacher.
:license: Python License.

## When to use

Reach for `ast` when your task calls for The `ast` module helps Python applications to process trees of the Python abstract syntax grammar. It ships with Python — no dependency to install, available on every interpreter.

## Import

```python
import ast
```

## Key functions

- `ast.compare(a, b, /, *, compare_attributes=False)`
- `ast.copy_location(new_node, old_node)`
- `ast.dump(node, annotate_fields=True, include_attributes=False, *, indent=None, show_empty=False)`
- `ast.fix_missing_locations(node)`
- `ast.get_docstring(node, clean=True)`
- `ast.get_source_segment(source, node, *, padded=False)`
- `ast.increment_lineno(node, n=1)`
- `ast.iter_child_nodes(node)`
- `ast.iter_fields(node)`
- `ast.literal_eval(node_or_string)`
- `ast.main(args=None)`
- `ast.parse(source, filename='<unknown>', mode='exec', *, type_comments=False, feature_version=None, optimize=-1)`
- `ast.unparse(ast_obj)`
- `ast.walk(node)`

## Key classes

`AST`, `Add`, `And`, `AnnAssign`, `Assert`, `Assign`, `AsyncFor`, `AsyncFunctionDef`, `AsyncWith`, `Attribute`, `AugAssign`, `AugLoad`, `AugStore`, `Await`, `BinOp`, `BitAnd`, `BitOr`, `BitXor`, `BoolOp`, `Break`, `Call`, `ClassDef`, `Compare`, `Constant`, `Continue`, `Del`, `Delete`, `Dict`, `DictComp`, `Div`

## Constants / attributes

`PyCF_ALLOW_TOP_LEVEL_AWAIT`, `PyCF_ONLY_AST`, `PyCF_OPTIMIZED_AST`, `PyCF_TYPE_COMMENTS`

## Structuring it in a program

Import once at the top of the module, then call into it where you need it. A
robust pattern wraps the call, handles the errors the module can raise, and
keeps the interface small:

```python
import ast

def do_work(...):
    """Use ast to accomplish one well-defined task."""
    result = ast.compare(...)
    return result
```

- Read the reference below for the exact signatures and exceptions.
- Prefer the highest-level function that does the job; drop to lower-level
  primitives only when you need the control.
- Keep `ast` calls behind a small function so the rest of your code does not
  depend on its details.

## Full reference (introspected from this machine)

```
Python Library Documentation: module ast

NAME
    ast

MODULE REFERENCE
    https://docs.python.org/3.14/library/ast.html

    The following documentation is automatically generated from the Python
    source files.  It may be incomplete, incorrect or include features that
    are considered implementation detail and may vary between Python
    implementations.  When in doubt, consult the module reference at the
    location listed above.

DESCRIPTION
    The `ast` module helps Python applications to process trees of the Python
    abstract syntax grammar.  The abstract syntax itself might change with
    each Python release; this module helps to find out programmatically what
    the current grammar looks like and allows modifications of it.

    An abstract syntax tree can be generated by passing `ast.PyCF_ONLY_AST` as
    a flag to the `compile()` builtin function or by using the `parse()`
    function from this module.  The result will be a tree of objects whose
    classes all inherit from `ast.AST`.

    A modified abstract syntax tree can be compiled into a Python code object
    using the built-in `compile()` function.

    Additionally various helper functions are provided that make working with
    the trees simpler.  The main intention of the helper functions and this
    module in general is to provide an easy to use interface for libraries
    that work tightly with the python syntax (template engines for example).

    :copyright: Copyright 2008 by Armin Ronacher.
    :license: Python License.

CLASSES
    builtins.object
        AST
            alias
            arg
            arguments
            boolop
                And
                Or
            cmpop
                Eq
                Gt
                GtE
                In
                Is
                IsNot
                Lt
                LtE
                NotEq
                NotIn
            comprehension
            excepthandler
                ExceptHandler
            expr
                Attribute
                Await
                BinOp
                BoolOp
                Call
                Compare
                Constant
                Dict
                DictComp
                FormattedValue
                GeneratorExp
                IfExp
                Interpolation
                JoinedStr
                Lambda
                List
                ListComp
                Name
                NamedExpr
                Set
                SetComp
                Slice
                Starred
                Subscript
                TemplateStr
                Tuple
                UnaryOp
                Yield
                YieldFrom
            expr_context
                AugLoad
                AugStore
                Del
                Load
                Param
                Store
            keyword
            match_case
            mod
                Expression
                FunctionType
                Interactive
                Module
                Suite
            operator
                Add
                BitAnd
                BitOr
                BitXor
                Div
                FloorDiv
                LShift
                MatMult
                Mod
                Mult
                Pow
                RShift
                Sub
            pattern
                MatchAs
                MatchClass
                MatchMapping
                MatchOr
                MatchSequence
                MatchSingleton
                MatchStar
                MatchValue
            slice
                ExtSlice
                Index
            stmt
                AnnAssign
                Assert
                Assign
                AsyncFor
                AsyncFunctionDef
                AsyncWith
                AugAssign
                Break
                ClassDef
                Continue
                Delete
                Expr
                For
                FunctionDef
                Global
                If
                Import
                ImportFrom
                Match
                Nonlocal
                Pass
                Raise
                Return
                Try
                TryStar
                TypeAlias
                While
                With
            type_ignore
                TypeIgnore
            type_param
                ParamSpec
                TypeVar
                TypeVarTuple
            unaryop
                Invert
                Not
                UAdd
                USub
            withitem
        NodeVisitor
            NodeTransformer

    class AST(builtins.object)
     |  Methods defined here:
     |
     |  __delattr__(self, name, /)
     |      Implement delattr(self, name).
     |
     |  __getattribute__(self, name, /)
     |      Return getattr(self, name).
     |
     |  __init__(self, /, *args, **kwargs)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  __reduce__(self, /)
     |      Helper for pickle.
     |
     |  __replace__(self, /, **fields)
     |      Return a copy of the AST node with new values for the specified fields.
     |
     |  __repr__(self, /)
     |      Return repr(self).
     |
     |  __setattr__(self, name, value, /)
     |      Implement setattr(self, name, value).
     |
     |  ----------------------------------------------------------------------
     |  Static methods defined here:
     |
     |  __new__(*args, **kwargs)
     |      Create and return a new object.  See help(type) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  __match_args__ = ()
     |
     |  _attributes = ()
     |
     | 
```

## Related

Other standard-library modules pair well with `ast`; explore the `python` domain of this catalog.
