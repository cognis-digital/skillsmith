---
name: python-keyword
description: "Program with Python's keyword module: Keywords (from 'Grammar/python.gram') This file is automatically generated; please don't muck it up!"
version: 1.0.0
tags: [keyword, programming, python, stdlib]
---

# Python: `keyword`

## Overview

Keywords (from "Grammar/python.gram")

This file is automatically generated; please don't muck it up!

To update the symbols in this file, 'cd' to the top directory of
the python source tree and run:

    PYTHONPATH=Tools/peg_generator python3 -m pegen.keywordgen         Grammar/python.gram         Grammar/Tokens         Lib/keyword.py

Alternatively, you can run 'make regen-keyword'.

## When to use

Reach for `keyword` when your task calls for Keywords (from "Grammar/python.gram") This file is automatically generated; please don't muck it up!. It ships with Python — no dependency to install, available on every interpreter.

## Import

```python
import keyword
```

## Key functions

- `keyword.iskeyword(object, /)`
- `keyword.issoftkeyword(object, /)`

## Constants / attributes

`kwlist`, `softkwlist`

## Structuring it in a program

Import once at the top of the module, then call into it where you need it. A
robust pattern wraps the call, handles the errors the module can raise, and
keeps the interface small:

```python
import keyword

def do_work(...):
    """Use keyword to accomplish one well-defined task."""
    result = keyword.iskeyword(...)
    return result
```

- Read the reference below for the exact signatures and exceptions.
- Prefer the highest-level function that does the job; drop to lower-level
  primitives only when you need the control.
- Keep `keyword` calls behind a small function so the rest of your code does not
  depend on its details.

## Full reference (introspected from this machine)

```
Python Library Documentation: module keyword

NAME
    keyword - Keywords (from "Grammar/python.gram")

MODULE REFERENCE
    https://docs.python.org/3.14/library/keyword.html

    The following documentation is automatically generated from the Python
    source files.  It may be incomplete, incorrect or include features that
    are considered implementation detail and may vary between Python
    implementations.  When in doubt, consult the module reference at the
    location listed above.

DESCRIPTION
    This file is automatically generated; please don't muck it up!

    To update the symbols in this file, 'cd' to the top directory of
    the python source tree and run:

        PYTHONPATH=Tools/peg_generator python3 -m pegen.keywordgen         Grammar/python.gram         Grammar/Tokens         Lib/keyword.py

    Alternatively, you can run 'make regen-keyword'.

FUNCTIONS
    iskeyword = __contains__(object, /) method of builtins.frozenset instance
        x.__contains__(y) <==> y in x.

    issoftkeyword = __contains__(object, /) method of builtins.frozenset instance
        x.__contains__(y) <==> y in x.

DATA
    __all__ = ['iskeyword', 'issoftkeyword', 'kwlist', 'softkwlist']
    kwlist = ['False', 'None', 'True', 'and', 'as', 'assert', 'async', 'aw...
    softkwlist = ['_', 'case', 'match', 'type']

FILE
    c:\python314\lib\keyword.py


```

## Related

Other standard-library modules pair well with `keyword`; explore the `python` domain of this catalog.
